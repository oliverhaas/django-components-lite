# The custom tag parser (parse_tag()) has been replaced with standard Django
# template tag parsing (token.split_contents() + FilterExpression).
# The advanced syntax (spread operators, inline dicts/lists, aggregate kwargs prefix)
# has been removed. This file is intentionally empty.
